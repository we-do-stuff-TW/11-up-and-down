# PROGRESS.md — 現在做到哪

> 這是留給「下一個工作階段」(未來的 Francis 或下一次啟動的 Claude)的斷點存檔。
> 更新時機:每次 ship 一個東西、或一個工作階段結束時。

## 目前狀態(一句話)

2026-09-22:設定跟著帳號走(語言／牌面／牌桌／速度／音效／名字寫進 `user_metadata.prefs`,換裝置重新登入拿回同一套;畫質留在裝置上)、預設立體牌桌、書房鏡頭拆掉(plan 0005)。**真帳號兩台裝置尚未實測。**
2026-09-22:線上多人「一輪結束卡著不動」「換局後手牌背面躺在桌上」已修(issue 0002、report 0003):立體桌接力補間改由舊到新處理;敲伺服器的人不在時其他人 4 秒後補敲;一趟請求最多等 12 秒。21 局 soak 量過,「越玩越當」不是漏記憶體。**改在 `docs/index.html`,尚未 push。**
2026-09-22:線上多人「叫完墩手牌換一副」已修——手牌不再寄生在房間那一列的 `rev` 上,而且張數對不上會自己去抓真的那一份(issue 0001、plan 0004)。
2026-09-15:介面有中英兩種語言了,設定裡切換、預設跟著瀏覽器。立體牌桌有一層後處理(接觸陰影、軟陰影、暗角、靜止時精算)與材質／幾何細節,畫質分三檔會自己降;底片顆粒那一層 Francis 看過之後決定不要,已拿掉。牌面圖集有一條「畫在牌面圖到齊之前而且永遠不補」的 bug(整副牌只剩角標)已修。文件系統仍由模板初始化,ARCHITECTURE / PRD 尚未回填。

## 下一步

1. `agent/ARCHITECTURE.md` 的技術棧、Codemap、架構不變量還是模板,沒回填。
2. 大廳的「特別感謝 Thomas Sun」三案做完了但 Francis 說先不放,
   原型在 `design/thanks/`(**刻意不進版控**,裡面有一張認得出是誰的照片,這個 repo 是公開的)。

## 時間軸(新的在上)

| 日期 | Ship 了什麼 |
|---|---|
| 2026-09-22 | 設定跟著帳號走、預設立體、拆書房鏡頭(plan 0005) |
| 2026-09-22 | 線上多人卡住三件事(issue 0002、report 0003):接力補間倒序 bug、ticker 不在時補敲、請求逾時;soak 證明不是漏記憶體 |
| 2026-09-22 | 手牌對帳：修掉「叫完墩手牌換一副」(issue 0001、plan 0004);手牌脫離 `rev`,張數對不上會自己抓回來 |
| 2026-09-15 | 顆粒拿掉、暗角收斂；修掉「牌面中間空白」的 bug(report 0002) |
| 2026-09-14 | 立體牌桌仿真度：後處理六層＋畫質三檔(plan 0003、ADR 0001、report 0001) |
| 2026-09-14 | 全英文版本：整個介面兩種語言，設定裡可切換(plan 0002) |
| 2026-09-13 | 文件系統初始化(套用 agent/ 文件模板) |
