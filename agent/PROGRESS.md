# PROGRESS.md — 現在做到哪

> 這是留給「下一個工作階段」(未來的 Francis 或下一次啟動的 Claude)的斷點存檔。
> 更新時機:每次 ship 一個東西、或一個工作階段結束時。

## 目前狀態(一句話)

2026-09-14:介面有中英兩種語言了,設定裡切換、預設跟著瀏覽器。立體牌桌加了一層後處理(接觸陰影、軟陰影、暗角顆粒、靜止時精算)與材質／幾何細節,畫質分三檔會自己降。文件系統仍由模板初始化,ARCHITECTURE / PRD 尚未回填。

## 下一步

1. 在真 GPU 上量立體牌桌每格的毫秒數(無頭 Chrome 走 swiftshader,量不出有意義的絕對值)。
   看設定頁「畫質」那一行,或 console 打 `UD.fx()`。
2. `agent/ARCHITECTURE.md` 的技術棧、Codemap、架構不變量還是模板,沒回填。

## 時間軸(新的在上)

| 日期 | Ship 了什麼 |
|---|---|
| 2026-09-14 | 立體牌桌仿真度：後處理六層＋畫質三檔(plan 0003、ADR 0001、report 0001) |
| 2026-09-14 | 全英文版本：整個介面兩種語言，設定裡可切換(plan 0002) |
| 2026-09-13 | 文件系統初始化(套用 agent/ 文件模板) |
